apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nginx-broken
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/thucpn/learn-architect
    targetRevision: main
    path: apps/nginx-broken
  destination:
    server: https://kubernetes.default.svc
    namespace: broken-test
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
