apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../base

patches:
- target:
    kind: Deployment
    name: nginx-multi-env
  patch: |-
    - op: replace
      path: /spec/replicas
      value: 5

nameSuffix: "-prod"
labels:
- pairs:
    env: prod
